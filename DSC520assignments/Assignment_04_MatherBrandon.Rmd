---
html_document: default
author: "Brandon Mather"
date: "`r Sys.Date()`"
output:
  pdf_document: default
  html_document:
    df_print: paged
word_document: default
title: "ASSIGNMENT 4"
bibliography: bibliography.bib
---
  
  # Markdown Basics
  
  ## Favorite Foods
1. Spaghetti
2. Pizza
3. Chipotle

  ## Images
![All Cases (Log Plot)](completed/assignment04/plots/10-all-cases-log.png)  
  ## Add a Quote
> Do…or do not. There is no try.   
  ## Add an Equation
$ E = mc^2$  
## Add a Footnote

This is a footnote [^1]

## Add Citations

* R for Everyone (@Lander2014)
* Discovering Statistics Using R (@Field,Miles, and Field 2012)

# Inline Code
```{r echo=TRUE, include=FALSE} 
library(ggplot2)
heights_df <- read.csv("data/r4ds/heights.csv")
covid_df <- read.csv("data/nytimes/covid-19-data/us-states.csv")
california_dfcalifornia_df <- covid_df[ which( covid_df$state == "California"), ]
florida_df <- covid_df[ which( covid_df$state == "Florida"), ]
ny_df <- covid_df[ which( covid_df$state == "New York"), ]
```
## NY Times COVID-19 Data
```{r echo=FALSE}
library(knitr)
library(latexpdf)
library(tinytex)
ggplot(data=florida_df, aes(x=date, group=1)) +
  geom_line(aes(y = cases, colour = "Florida")) +
  geom_line(data=ny_df, aes(y = cases,colour="New York")) +
  geom_line(data=california_df, aes(y = cases, colour="California")) +
  scale_colour_manual("",
                      breaks = c("Florida", "New York", "California"),
                      values = c("darkred", "darkgreen", "steelblue")) +
  xlab(" ") + ylab("Cases") + scale_y_log10()
```
## R4DS Height vs Earnings
```{r echo=FALSE}
ggplot(heights_df, aes(x=height, y=earn, col=sex)) + geom_point() +xlab("Height (Inches)") + ylab("Earning (Dollars)")
```
# Tables

## Knitr Table with Kable
```{r echo=FALSE}
library(knitr)
name <- c("Aragon", "Bilbo", "Frodo", "Sam", "Sauron")
race <- c("Men", "Hobbit", "Hobbit", "Hobbit", "Maia")
in_fellowship <- c(TRUE, FALSE, TRUE, TRUE, FALSE)
ring_bearer <- c(FALSE, TRUE, TRUE, TRUE, TRUE)
age <- c(88, 129, 51, 36, 7052)
characters_df <- data.frame(name, race, in_fellowship, ring_bearer, age)
knitr::kable(characters_df,row.names = NA,caption ="One Ring to Rule Them All" )
```
## Pandoc Table
```{r message = FALSE, warning = FALSE, echo = FALSE}
pandoc.table(characters_df,'One Ring to Rule Them All',col.names=c("Name","Race","In Fellowship? ","Is Ring Bearer?","Age"))
```
# References
